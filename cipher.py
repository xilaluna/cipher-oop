from UserMenu import UserMenu

user_cipher_choice = input(
    'Which cipher would you like to use to decode/encrypt :')

user_cryptography = input('Which cipher would you like to choose')

user = UserMenu(user_cipher_choice)
